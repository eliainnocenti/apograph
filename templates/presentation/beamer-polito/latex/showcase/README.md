# Showcase support files

These slides are loaded only by the root `showcase.tex` preview entry point.
They are not part of the minimal `main.tex` starter flow.

For an ordinary presentation, edit `content/slides.tex`. You may safely remove
both `showcase.tex` and this directory when you no longer need the component
guide.
