% Optional appendix example preserved from upstream.
% SPDX-License-Identifier: CC-BY-4.0

\chapter{Galileo}
\label{sec:appendix_galileo}

\lstdefinelanguage{JavaScript}{
  keywords={break, case, catch, continue, debugger, default, delete, do, else, finally, for, function, if, in, const, instanceof, new, return, switch, this, throw, try, typeof, var, void, while, with},
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]",
  sensitive=true
}

% \lstinputlisting[]{} can include a source file directly.
\begin{lstlisting}[language=JavaScript]
    const test = test;
\end{lstlisting}

% Computational complexity.
$\mathcal{O}\left(n\log{n}\right)$

% Inline verbatim.
\verb+numpy+
